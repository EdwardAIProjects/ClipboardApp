# Clipboard

Android app that adds a `Clipboard` option to the share page.

Share text, links, or files to it and they are copied to your clipboard immediately.

## Build

```bash
./gradlew :app:assembleDebug
```

APK:

```text
app/build/outputs/apk/debug/app-debug.apk
```
